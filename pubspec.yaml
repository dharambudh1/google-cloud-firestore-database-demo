name: firestore_database_demo
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  cloud_firestore: ^4.15.8
  cupertino_icons: ^1.0.2
  firebase_core: ^2.27.0
  flutter:
    sdk: flutter
  get: ^4.6.6
  keyboard_dismisser: ^3.0.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
